#include<iostream>
#include<string>

using namespace std;
int DP[1000][1000];
int main(){
  int n;
  scanf("%d", &n);
  vector<vector<int> > path(11);
  while(1){    
  string input;  
  getline(cin, input);
  if (input[0] == 0)
    break;
  stringstream ss(input);
  int k;
  while(ss >> k){
    path[i].push_back(k);
  }
  i++;
  }
  for(int k = 1; k <= count; k++){
    

  for(int i = 1; i < path[0].size()+1; i++){
    for(int j = 1; j < path[k].size()+1; k++){
      if (path[0][i-1] == path[k][j-1])
        DP[i][j] = 1+DP[i-1][j-1];
      else
        DP[i][j] = max(DP[i][j-1], DP[i-1][j]);
    }
  }
      
    

  

  
  
  
  
  
