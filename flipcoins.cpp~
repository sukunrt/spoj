#include <iostream>
#include <math>

using namespace std;
int A[100002];
int main(){
  int i,j,N;
  scanf("%d", &N);

  memset(A, 0, sizeof(A));

}

void update(int *M, int begin, int end, int i, int j, int n){
  if (begin >= i && end <= j){

  }
  else if(begin >= end){

  }
  else{
    int mid = (start+end)/2;

  }
}
    

  
  
